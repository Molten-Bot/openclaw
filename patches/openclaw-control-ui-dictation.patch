diff --git a/ui/src/styles/chat/layout.css b/ui/src/styles/chat/layout.css
index 5be268b0..b602e7e9 100644
--- a/ui/src/styles/chat/layout.css
+++ b/ui/src/styles/chat/layout.css
@@ -572,6 +572,7 @@
 }
 
 .agent-chat__composer-combobox {
+  position: relative;
   display: flex;
   flex-direction: column;
 }
@@ -612,6 +613,63 @@
   color: var(--muted);
 }
 
+.agent-chat__dictation-waveform {
+  position: absolute;
+  inset: 0;
+  display: flex;
+  align-items: center;
+  justify-content: stretch;
+  gap: 4px;
+  padding: 8px 14px;
+  pointer-events: none;
+  background: color-mix(in srgb, var(--card) 88%, transparent);
+  border-radius: var(--radius-lg) var(--radius-lg) 0 0;
+  overflow: hidden;
+}
+
+.agent-chat__dictation-waveform::before {
+  content: "";
+  position: absolute;
+  left: 14px;
+  right: 14px;
+  top: 50%;
+  height: 1px;
+  background: color-mix(in srgb, var(--accent) 34%, transparent);
+}
+
+.agent-chat__dictation-bar {
+  position: relative;
+  z-index: 1;
+  flex: 1 1 0;
+  min-width: 2px;
+  height: var(--bar-height, 18%);
+  max-height: 100%;
+  border-radius: 999px;
+  background: color-mix(in srgb, var(--accent) 80%, var(--text));
+  opacity: 0.78;
+  transform-origin: center;
+  transition: height 80ms ease-out;
+  animation: dictationWaveIdle 720ms ease-in-out infinite alternate;
+  animation-delay: var(--bar-delay, 0ms);
+}
+
+@keyframes dictationWaveIdle {
+  from {
+    opacity: 0.52;
+    transform: scaleY(0.88);
+  }
+  to {
+    opacity: 0.95;
+    transform: scaleY(1.08);
+  }
+}
+
+@media (prefers-reduced-motion: reduce) {
+  .agent-chat__dictation-bar {
+    animation: none;
+  }
+}
+
 .agent-chat__stt-interim {
   padding: 10px 14px 0;
   color: var(--muted);
@@ -686,6 +744,10 @@
   background: color-mix(in srgb, var(--danger, #ef4444) 14%, transparent);
 }
 
+.agent-chat__input-btn--dictate {
+  margin-left: auto;
+}
+
 .agent-chat__talk-status {
   color: var(--text);
 }
diff --git a/ui/src/ui/app-lifecycle.ts b/ui/src/ui/app-lifecycle.ts
index b36179ac..8bd9bf38 100644
--- a/ui/src/ui/app-lifecycle.ts
+++ b/ui/src/ui/app-lifecycle.ts
@@ -42,6 +42,8 @@ type LifecycleHost = {
   realtimeTalkStatus?: string;
   realtimeTalkDetail?: string | null;
   realtimeTalkTranscript?: string | null;
+  dictationActive?: boolean;
+  toggleDictation?: () => void;
   chatLoading: boolean;
   chatMessages: unknown[];
   chatToolMessages: unknown[];
@@ -132,6 +134,9 @@ export function handleDisconnected(host: LifecycleHost) {
   host.realtimeTalkStatus = "idle";
   host.realtimeTalkDetail = null;
   host.realtimeTalkTranscript = null;
+  if (host.dictationActive) {
+    host.toggleDictation?.();
+  }
   host.client?.stop();
   host.client = null;
   host.connected = false;
diff --git a/ui/src/ui/app-render.ts b/ui/src/ui/app-render.ts
index afec74ce..85692ce3 100644
--- a/ui/src/ui/app-render.ts
+++ b/ui/src/ui/app-render.ts
@@ -2486,6 +2486,10 @@ export function renderApp(state: AppViewState) {
                   realtimeTalkTranscript: state.realtimeTalkTranscript,
                   realtimeTalkOptionsOpen: state.realtimeTalkOptionsOpen,
                   realtimeTalkOptions: state.realtimeTalkOptions,
+                  dictationActive: state.dictationActive,
+                  dictationSupported: state.dictationSupported,
+                  dictationError: state.dictationError,
+                  dictationLevels: state.dictationLevels,
                   connected: state.connected,
                   canSend: state.connected,
                   disabledReason: chatDisabledReason,
@@ -2532,6 +2536,7 @@ export function renderApp(state: AppViewState) {
                     state.realtimeTalkOptionsOpen = !state.realtimeTalkOptionsOpen;
                   },
                   onRealtimeTalkOptionsChange: (next) => state.updateRealtimeTalkOptions(next),
+                  onToggleDictation: () => state.toggleDictation(),
                   canAbort: hasAbortableSessionRun(state),
                   onAbort: () => void state.handleAbortChat({ preserveDraft: true }),
                   onQueueRemove: (id) => state.removeQueuedMessage(id),
diff --git a/ui/src/ui/app-view-state.ts b/ui/src/ui/app-view-state.ts
index 7138c46f..aa33f88a 100644
--- a/ui/src/ui/app-view-state.ts
+++ b/ui/src/ui/app-view-state.ts
@@ -135,6 +135,11 @@ export type AppViewState = {
     reasoningEffort: string;
   };
   updateRealtimeTalkOptions: (next: Partial<AppViewState["realtimeTalkOptions"]>) => void;
+  dictationActive: boolean;
+  dictationSupported: boolean;
+  dictationError: string | null;
+  dictationLevels: number[];
+  toggleDictation: () => void;
   chatManualRefreshInFlight: boolean;
   chatHeaderControlsHidden: boolean;
   chatMobileControlsOpen: boolean;
diff --git a/ui/src/ui/app.ts b/ui/src/ui/app.ts
index 0c9d33d4..bfd675c8 100644
--- a/ui/src/ui/app.ts
+++ b/ui/src/ui/app.ts
@@ -130,9 +130,38 @@ import type { NostrProfileFormState } from "./views/channels.nostr-profile-form.
 declare global {
   interface Window {
     __OPENCLAW_CONTROL_UI_BASE_PATH__?: string;
+    SpeechRecognition?: SpeechRecognitionConstructor;
+    webkitSpeechRecognition?: SpeechRecognitionConstructor;
+    webkitAudioContext?: typeof AudioContext;
   }
 }
 
+type SpeechRecognitionConstructor = new () => SpeechRecognitionLike;
+
+type SpeechRecognitionLike = EventTarget & {
+  continuous: boolean;
+  interimResults: boolean;
+  lang: string;
+  onend: (() => void) | null;
+  onerror: ((event: SpeechRecognitionErrorEventLike) => void) | null;
+  onresult: ((event: SpeechRecognitionResultEventLike) => void) | null;
+  start: () => void;
+  stop: () => void;
+};
+
+type SpeechRecognitionErrorEventLike = Event & {
+  error?: string;
+  message?: string;
+};
+
+type SpeechRecognitionResultEventLike = Event & {
+  resultIndex: number;
+  results: ArrayLike<{
+    isFinal: boolean;
+    0?: { transcript?: string };
+  }>;
+};
+
 const bootAssistantIdentity = normalizeAssistantIdentity({});
 const bootLocalUserIdentity = loadLocalUserIdentity();
 
@@ -247,6 +276,20 @@ export class OpenClawApp extends LitElement {
     reasoningEffort: "",
   };
   private realtimeTalkSession: RealtimeTalkSession | null = null;
+  @state() dictationActive = false;
+  @state() dictationSupported =
+    typeof window.SpeechRecognition === "function" ||
+    typeof window.webkitSpeechRecognition === "function";
+  @state() dictationError: string | null = null;
+  @state() dictationLevels = Array.from({ length: 40 }, () => 0.18);
+  private dictationRecognition: SpeechRecognitionLike | null = null;
+  private dictationBaseDraft = "";
+  private dictationFinalText = "";
+  private dictationManuallyStopped = false;
+  private dictationAudioContext: AudioContext | null = null;
+  private dictationAudioStream: MediaStream | null = null;
+  private dictationAnalyser: AnalyserNode | null = null;
+  private dictationAnimationFrame: number | null = null;
   private nativeBridgeCleanup: (() => void) | null = null;
   @state() chatManualRefreshInFlight = false;
   @state() chatHeaderControlsHidden = false;
@@ -976,6 +1019,139 @@ export class OpenClawApp extends LitElement {
     );
   }
 
+  private getSpeechRecognitionConstructor(): SpeechRecognitionConstructor | null {
+    return window.SpeechRecognition ?? window.webkitSpeechRecognition ?? null;
+  }
+
+  private stopDictationWaveform() {
+    if (this.dictationAnimationFrame != null) {
+      window.cancelAnimationFrame(this.dictationAnimationFrame);
+      this.dictationAnimationFrame = null;
+    }
+    this.dictationAudioStream?.getTracks().forEach((track) => track.stop());
+    this.dictationAudioStream = null;
+    this.dictationAnalyser = null;
+    void this.dictationAudioContext?.close();
+    this.dictationAudioContext = null;
+    this.dictationLevels = Array.from({ length: 40 }, () => 0.18);
+  }
+
+  private async startDictationWaveform() {
+    if (!navigator.mediaDevices?.getUserMedia) {
+      return;
+    }
+    try {
+      const stream = await navigator.mediaDevices.getUserMedia({ audio: true });
+      if (!this.dictationActive) {
+        stream.getTracks().forEach((track) => track.stop());
+        return;
+      }
+      const AudioContextConstructor = window.AudioContext ?? window.webkitAudioContext;
+      const audioContext = new AudioContextConstructor();
+      const analyser = audioContext.createAnalyser();
+      analyser.fftSize = 128;
+      analyser.smoothingTimeConstant = 0.78;
+      audioContext.createMediaStreamSource(stream).connect(analyser);
+      this.dictationAudioStream = stream;
+      this.dictationAudioContext = audioContext;
+      this.dictationAnalyser = analyser;
+      const data = new Uint8Array(analyser.frequencyBinCount);
+      const renderFrame = () => {
+        if (!this.dictationAnalyser || !this.dictationActive) {
+          return;
+        }
+        this.dictationAnalyser.getByteFrequencyData(data);
+        const bucketCount = 40;
+        const bucketSize = Math.max(1, Math.floor(data.length / bucketCount));
+        this.dictationLevels = Array.from({ length: bucketCount }, (_, index) => {
+          const start = index * bucketSize;
+          const end = Math.min(data.length, start + bucketSize);
+          let total = 0;
+          for (let i = start; i < end; i += 1) {
+            total += data[i] ?? 0;
+          }
+          return Math.max(0.08, Math.min(1, total / Math.max(1, end - start) / 255));
+        });
+        this.dictationAnimationFrame = window.requestAnimationFrame(renderFrame);
+      };
+      renderFrame();
+    } catch {
+      // Speech recognition can still run even when analyser setup fails.
+    }
+  }
+
+  private stopDictation() {
+    this.dictationManuallyStopped = true;
+    this.dictationRecognition?.stop();
+    this.dictationRecognition = null;
+    this.dictationActive = false;
+    this.stopDictationWaveform();
+  }
+
+  toggleDictation() {
+    if (this.dictationActive) {
+      this.stopDictation();
+      return;
+    }
+    const Recognition = this.getSpeechRecognitionConstructor();
+    if (!Recognition) {
+      this.dictationSupported = false;
+      this.dictationError = "Dictation is not supported in this browser.";
+      this.lastError = this.dictationError;
+      return;
+    }
+    const recognition = new Recognition();
+    recognition.continuous = true;
+    recognition.interimResults = true;
+    recognition.lang = navigator.language || "en-US";
+    this.dictationBaseDraft = this.chatMessage;
+    this.dictationFinalText = "";
+    this.dictationError = null;
+    this.dictationManuallyStopped = false;
+    recognition.onresult = (event) => {
+      let interim = "";
+      for (let i = event.resultIndex; i < event.results.length; i += 1) {
+        const result = event.results[i];
+        const text = result?.[0]?.transcript ?? "";
+        if (result?.isFinal) {
+          this.dictationFinalText = `${this.dictationFinalText}${text}`;
+        } else {
+          interim = `${interim}${text}`;
+        }
+      }
+      const prefix = this.dictationBaseDraft.trimEnd();
+      const dictated = `${this.dictationFinalText}${interim}`.trimStart();
+      this.chatMessage = [prefix, dictated].filter(Boolean).join(" ");
+      resetChatInputHistoryNavigationInternal(
+        this as unknown as Parameters<typeof resetChatInputHistoryNavigationInternal>[0],
+      );
+    };
+    recognition.onerror = (event) => {
+      this.dictationError = event.message || event.error || "Dictation failed.";
+      this.lastError = this.dictationError;
+    };
+    recognition.onend = () => {
+      this.dictationRecognition = null;
+      this.dictationActive = false;
+      this.stopDictationWaveform();
+      if (!this.dictationManuallyStopped && !this.dictationError) {
+        this.dictationError = "Dictation stopped.";
+      }
+    };
+    this.dictationRecognition = recognition;
+    this.dictationActive = true;
+    void this.startDictationWaveform();
+    try {
+      recognition.start();
+    } catch (error) {
+      this.dictationRecognition = null;
+      this.dictationActive = false;
+      this.stopDictationWaveform();
+      this.dictationError = error instanceof Error ? error.message : String(error);
+      this.lastError = this.dictationError;
+    }
+  }
+
   updateRealtimeTalkOptions(next: Partial<typeof this.realtimeTalkOptions>) {
     this.realtimeTalkOptions = { ...this.realtimeTalkOptions, ...next };
   }
diff --git a/ui/src/ui/views/chat.test.ts b/ui/src/ui/views/chat.test.ts
index aeb2e673..bd3c74b2 100644
--- a/ui/src/ui/views/chat.test.ts
+++ b/ui/src/ui/views/chat.test.ts
@@ -538,6 +538,23 @@ describe("chat voice controls", () => {
     expect(container.querySelector('[aria-label="Voice input"]')).toBeNull();
   });
 
+  it("renders dictation button at the compose controls and speech waveform over the prompt", () => {
+    const container = renderChatView({
+      dictationActive: true,
+      dictationSupported: true,
+      dictationLevels: [0.2, 0.8],
+      onToggleDictation: () => undefined,
+    });
+
+    requireElement(container, '[aria-label="Stop dictation"]', "Stop dictation button");
+    const waveform = requireElement(
+      container,
+      ".agent-chat__composer-combobox > .agent-chat__dictation-waveform",
+      "dictation waveform",
+    );
+    expect(waveform.querySelectorAll(".agent-chat__dictation-bar")).toHaveLength(2);
+  });
+
   it("renders editable Talk launch options", () => {
     const onRealtimeTalkOptionsChange = vi.fn();
     const container = renderChatView({
diff --git a/ui/src/ui/views/chat.ts b/ui/src/ui/views/chat.ts
index 741e8ad1..bc208f70 100644
--- a/ui/src/ui/views/chat.ts
+++ b/ui/src/ui/views/chat.ts
@@ -88,6 +88,10 @@ export type ChatProps = {
     prefixPaddingMs: string;
     reasoningEffort: string;
   };
+  dictationActive?: boolean;
+  dictationSupported?: boolean;
+  dictationError?: string | null;
+  dictationLevels?: number[];
   connected: boolean;
   canSend: boolean;
   disabledReason: string | null;
@@ -123,6 +127,7 @@ export type ChatProps = {
   onOpenSessionCheckpoints?: () => void | Promise<void>;
   onToggleRealtimeTalk?: () => void;
   onToggleRealtimeTalkOptions?: () => void;
+  onToggleDictation?: () => void;
   onRealtimeTalkOptionsChange?: (
     next: Partial<NonNullable<ChatProps["realtimeTalkOptions"]>>,
   ) => void;
@@ -273,6 +278,28 @@ function renderRealtimeTalkOptions(props: ChatProps) {
   `;
 }
 
+function renderDictationWaveform(props: ChatProps) {
+  if (!props.dictationActive) {
+    return nothing;
+  }
+  const levels = props.dictationLevels?.length
+    ? props.dictationLevels
+    : Array.from({ length: 40 }, () => 0.18);
+  return html`
+    <div class="agent-chat__dictation-waveform" aria-hidden="true">
+      ${levels.map((level, index) => {
+        const height = Math.max(10, Math.round(level * 100));
+        return html`
+          <span
+            class="agent-chat__dictation-bar"
+            style=${`--bar-height:${height}%; --bar-delay:${index * 18}ms`}
+          ></span>
+        `;
+      })}
+    </div>
+  `;
+}
+
 interface ChatEphemeralState {
   slashMenuOpen: boolean;
   slashMenuItems: SlashCommandDef[];
@@ -1394,6 +1421,7 @@ export function renderChat(props: ChatProps) {
             placeholder=${placeholder}
             rows="1"
           ></textarea>
+          ${renderDictationWaveform(props)}
           <span
             id=${SLASH_MENU_ACTIVE_ANNOUNCEMENT_ID}
             class="agent-chat__sr-only"
@@ -1451,6 +1479,26 @@ export function renderChat(props: ChatProps) {
             ${tokens ? html`<span class="agent-chat__token-count">${tokens}</span>` : nothing}
           </div>
 
+          ${props.onToggleDictation
+            ? html`
+                <button
+                  class="agent-chat__input-btn agent-chat__input-btn--dictate ${props.dictationActive
+                    ? "agent-chat__input-btn--active"
+                    : ""}"
+                  @click=${props.onToggleDictation}
+                  title=${props.dictationActive
+                    ? "Stop dictation"
+                    : props.dictationSupported === false
+                      ? "Dictation is not supported in this browser"
+                      : "Start dictation"}
+                  aria-label=${props.dictationActive ? "Stop dictation" : "Start dictation"}
+                  aria-pressed=${props.dictationActive ? "true" : "false"}
+                  ?disabled=${!props.connected || props.dictationSupported === false}
+                >
+                  ${props.dictationActive ? icons.micOff : icons.mic}
+                </button>
+              `
+            : nothing}
           ${renderChatRunControls({
             canAbort,
             connected: props.connected,
